# Acceptance evidence

This records isolated native acceptance scenarios for Beads, Tollgate, source selection, and bead-linked observation. No production Hive or Fulcrum state was changed. An eligible inactive disposable thread was unavailable for archival. The Tollgate provider correction was explicitly approved, promoted and installed after these journeys.

## Tollgate completion status, 2026-09-23

Provider candidate `01a0cead-dfd8-73a2-9b0f-6976155138f0` passed validation and was explicitly approved. Tollgate reported `promoted`, remote `synchronized` and cleanup `completed`, with certificate `01a0ceae-7bdd-7b82-8370-6ab766a232d6`. The certified release `d6f14f827772937d05eb6d9c1faf3876762fffef` was built in a detached worktree, code signed, installed and restarted. The installed app and CLI binaries matched the certified build. Native candidate status from the running app reported `local_master.status=synchronized`, `contains_tested=true`, `policy_enabled=true` and local master commit `d6f14f827772937d05eb6d9c1faf3876762fffef`. `tg --no-launch doctor` with Tollgate's repository ID returned `healthy=true` with all checks healthy.

## Manual backup and restore, 2026-09-23

Two disposable Beads 1.2.2 server-mode stores on Dolt 2.2.0 were used. `bd backup init <isolated path> --json`, `bd backup sync --json`, then `bd backup restore <isolated path> --force --json` in the second initialized store succeeded. The round trip took 6.08 seconds. Native `list --all --limit 0 --json` agreed for two beads on title, description, acceptance criteria, priority, flat metadata, historical assignee, status and the dependency edge. Native `comments <id> --json` agreed for the recorded note. The first bead carried `hive_resolution=completed` and a closed historical assignee. The disposable IDs were `hv-bf1` and `hv-6h8`. No production store was touched.

## Native dependent executor journey, 2026-09-23

In a disposable registered repository with a local bare remote, isolated Beads server and actual Codex thread `01a0ce84-c2c9-7d23-a621-b3bd114675f0`, the native `bd` commands filed `hv-hy3` and dependent `hv-xxo`. Both stored that thread as `hive_origin_thread`; the second initially had no `hive_project` and was explicitly corrected before work. `bd ready --json` showed only `hv-hy3`. Native `bd --actor <actual-thread-id> update ID --claim` set the assignee on each bead in order. Each change was made in a Tollgate-created worktree, cold-reviewed by a fresh subagent, committed with a Conventional Commit and submitted with `tg candidate HEAD --json`. `tg approve ID --wait --json` returned terminal `promoted`, `remote_state=synchronized`, `cleanup_state=completed` for candidates `01a0ce9b-6968-72e1-a1a4-4eeec469a428` and `01a0ce9d-5670-7013-8a7c-75a0badef7e7`. Tested commits were `f9d7362afde790962be5378ec3b6b719ca6a03e0` and `8579238cec0fdf1a0ac55ab929b8a676afba2374`. The first was closed with `hive_resolution=completed` before `bd ready` exposed the second; the second was closed the same way after delivery. `git rev-parse master refs/remotes/origin/master` and `git ls-remote origin refs/heads/master` all returned the second tested commit. The native journey took about 3 minutes 15 seconds from filing to second delivery, including agent review and model/tool time; individual native commands completed in less than a second. Beads 1.2.2, Tollgate 0.1.0 and Dolt 2.2.0 were used. The setup was disposable and no production task or remote was touched.

The first journey used one real executor thread. A separate disposable native claim race with two distinct real Codex threads is recorded below. A third candidate (`01a0ce9f-1063-72e0-b51f-85b74a1f8ec8`) completed the one-time real 30-minute native wait and source-update check.

The command log reconstructs at least 19 task-path native Beads/Tollgate invocations across the two deliveries. The overlapping Codex transcript window recorded 22 model response records and 21 tool calls, including provider investigation outside this journey. These counts and wall time describe one observed workflow, not a matched performance comparison. In a separate empty disposable state, the first `hive source` call selected the committed source in 0.159 seconds; two later calls took 0.0533 and 0.0527 seconds. The observed thread-level API-equivalent cost and coverage are below; there is no per-bead allocation.

A later native `bd update hv-hy3 --title 'First disposable delivery (renamed)'` succeeded on the closed first bead and `bd show --json` confirmed the new title with status still closed. This verified direct rename, although it was performed after the second delivery rather than between the two claims.

## Ordered repeat of the short journey, 2026-09-23

To close the ordering gap above, the same disposable registered repository and Beads store filed `hv-ctv` and dependent `hv-e0u` with the actual creator thread ID. The dependent initially lacked `hive_project`; native `bd dep add hv-e0u hv-ctv` established the edge before that key was added. `bd ready --json` exposed only `hv-ctv`. The actual thread claimed it, created a Tollgate worktree and delivered `ordered-first.txt` while removing the completed long-wait marker. Fresh cold review and `./ci` passed. Candidate `01a0cebe-bff9-7880-b492-eace6cb143c7` promoted tested commit `f2c7ce8280a5be89c12619f6c2de0c86ec506ae0` with synchronized remote and completed cleanup. The executor recorded `hive_resolution=completed` and closed `hv-ctv`, then renamed the **bead title** with native `bd update --title`. Only after that did `bd ready --json` expose `hv-e0u`; the same actual thread claimed it. A second Tollgate-created worktree added `ordered-second.txt`; fresh cold review and `./ci` passed. Candidate `01a0cebf-9e5e-7e13-a2db-d62ff0eca983` promoted tested commit `a376ef8d78ddb2ca0c5f0eabb6b51497aed6d23e` with synchronized remote and completed cleanup, and `hv-e0u` closed with `hive_resolution=completed`. Native `bd show hv-ctv hv-e0u --json` retained creator, assignee, prerequisite and the renamed closed bead title. Local `master`, remote tracking ref and bare remote `master` all resolved to the second tested commit. This repeat took about 2 minutes 19 seconds from filing to final closure, including fresh review and model/tool time. The command record contains 20 task-path native Beads/Tollgate invocations; the same transcript interval (14:48:55–14:51:15 UTC) contains 21 model response records and 20 tool calls. Some model/tool activity in that interval was unrelated provider investigation. Beads 1.2.2, Tollgate 0.1.0 and Dolt 2.2.0 were used.

The workflow's required **Codex task title** transition was then exercised as an adjacent dependent pair: after `hv-e0u` was delivered and closed, native `set_thread_title` renamed actual thread `01a0ce84-c2c9-7d23-a621-b3bd114675f0` to `⚒️ Hive acceptance: next delivery`. Only afterward did it file dependent bead `hv-nrh`, inspect readiness, claim as that same thread, and deliver `title-followup.txt` through a Tollgate-created worktree and fresh cold review. Candidate `01a0cec3-351d-7213-ade2-81f496ad649e` promoted tested commit `89d01e044634da698818919baa2eaeada414a79a` with synchronized remote and completed cleanup; `hv-nrh` closed with `hive_resolution=completed`. This verifies direct native Codex title change before the next dependent claim and delivery. The first two-bead pair's bead-title rename alone did not test that UI action.

After the repeat and title follow-up, bounded observation through cursor byte 9,554,943 found 412 native unique response IDs in a separate read-only transcript scan and 412 derived response rows. The thread-level cost report showed 412 priced responses, zero unpriced or missing usage, standard-tier API-equivalent estimate $12.3752176, two disclosed parse gaps, no remaining bytes at that scan, fresh associations for six disposable beads, and one total for the shared thread. This is the full linked thread's observed cost, including unrelated work; it is not a per-journey or per-bead allocation.

## Local gate budgets, 2026-09-23

Three consecutive quiescent runs after the routing fix passed. Wall times from `/usr/bin/time -p` were 9.30, 9.89 and 8.86 seconds for `scripts/check-fast` (30-second budget), and 17.07, 17.10 and 17.35 seconds for `scripts/check` (120-second budget). The full gate includes native Beads server tests; hosted CI is asynchronous.

## Native observation and manual archive check, 2026-09-23

An isolated Hive source clone at `13830f86828cd18e43c3935d1e855076d5f94399` and isolated state read the disposable Beads store through `hive telemetry links --json`. It returned four links: creator and executor for each of the two closed beads, all pointing to the same real Codex thread. It reported no link gaps. `hive telemetry sweep --native-index /Users/dthurn/.codex/state_5.sqlite --json` collected the known thread in bounded 1 MiB batches until its cursor reached the then-current end. A separate read-only JSONL scan through the exact stored byte cursor counted 233 native `token_usage_record` records with 233 unique response IDs; the telemetry database had 233 responses. `hive cost --task <thread-id> --json` showed one thread total with all four bead relations, 233 priced responses, standard tier assumption, observed model `gpt-6-sol`, and an API-equivalent estimate of $6.6413548 at that scan. It also disclosed one skipped oversized transcript line; no response was lost in the cursor-aligned comparison.

The opt-in `hive telemetry watch --native-index ... --interval-seconds 5` ran twice under the disposable bootstrap configuration. Each run emitted one `CollectorBatchFinished`, accepted SIGTERM and exited 0; no host service was installed. A second cursor-aligned read-only scan after restart counted 236 unique native response IDs and the derived store had 236 rows and 236 distinct response IDs. Native Beads work was unaffected. The Codex task list identified the linked thread as **active**, so the manual archivist correctly skipped it. No eligible disposable thread was archived; that capability remains unproven until native inactivity evidence exists.

## Competing real-thread claim, 2026-09-23

The same isolated Beads store filed disposable bead `hv-gty` with the actual creator thread ID. The executor thread `01a0ce84-c2c9-7d23-a621-b3bd114675f0` and a fresh review thread `01a0ceae-d81c-74c0-8f60-ef2994d201ca` each waited on one filesystem marker, then both ran native `bd --actor <own-thread-id> update hv-gty --claim`. The executor's command succeeded (exit 0). The review thread's command returned exit 1, explicitly saying the issue was already claimed by the executor. Its read-only `bd show hv-gty --json` confirmed `in_progress` and the executor assignee; the losing thread made no task or file edits. The test bead was closed with a recorded completion resolution. This establishes the native one-owner primitive for two real Codex actors; it does not impose a Hive-wide capacity limit.

## Native wait and source update, 2026-09-23

Disposable candidate `01a0ce9f-1063-72e0-b51f-85b74a1f8ec8` tested commit `b6dc653d0b4c71d16b0fec6806c08615f401e20b` with a native CI command that sleeps 1,800 seconds when `long_wait.txt` exists. The actual Codex tool host started `tg approve ID --wait --json` at 2026-09-23 14:15:29.894 UTC, retained its process session, and retrieved terminal output at 14:45:48.994 UTC: 30 minutes 19.100 seconds of host-observed wall time. No model-driven candidate polls occurred during that interval. The isolated Hive source clone initially selected `13830f86828cd18e43c3935d1e855076d5f94399`. While the native wait was pending, `git fetch origin master` and `git merge --ff-only origin/master` advanced only that clone to `d330adb3a9c1c464541d5a78820fd6730644e604`, and a new `hive source` call selected the new commit. A retained opt-in collector process then emitted 14 successful batches selected from `d330adb3a9c1c464541d5a78820fd6730644e604`; after the same source clone advanced to `329cafe755da01124f930d19fea6e68dc3cddec1`, that same process emitted four successful batches selected from the new commit before clean SIGTERM. Source consistency within one running call is covered separately by the source-selection black-box test in `scripts/check`. The native wait returned `promoted`, `remote_state=synchronized`, and `cleanup_state=completed` after provider-reported `elapsed_ms=1800728` (30 minutes and 0.728 seconds of validation). Exact tested commit `b6dc653d0b4c71d16b0fec6806c08615f401e20b` was present at local `master`, `refs/remotes/origin/master`, and the bare remote's `master`.
