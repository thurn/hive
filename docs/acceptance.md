# Acceptance evidence

This records isolated native acceptance scenarios for Beads, Tollgate, source selection, and bead-linked observation. No production Hive or Fulcrum state was changed. An eligible inactive disposable thread was unavailable for archival. The Tollgate provider correction was explicitly approved, promoted and installed after these journeys.

## Claude cost implementation, 2026-09-24

All twelve steps of [the cost plan](claude-cost-plan.md) are implemented as separate Conventional Commits. Steps 1–11 passed Tollgate and were synchronized individually to remote `master`; the final documentation commit records this evidence. The expanded full gate passed step 11 (`7be577198b74d720ecc1faae1ee6692b505f0bdd`) in 127.337 seconds. Its limit is now 180 seconds after the added native scenarios exceeded the previous 120-second limit; `check-fast` remains 30 seconds and collection remains five seconds with two-second Beads/event budgets.

The [machine-readable observations](measurements/claude-cost-20260924.json) contain amounts, coverage and checks only, without prompts, tool outputs, credentials or raw exports. Tests used private observation stores, disposable bd 1.2.2/Dolt servers and process-only Claude telemetry settings. No persistent Claude settings, organization settings, production collector activation or production Beads data changed.

| Plan acceptance | Evidence and disposition |
| --- | --- |
| Arithmetic | Real Claude Code 2.1.282 nested-agent session `c7f89d5c` produced five requests at exactly `$0.069755`; transcript pricing, summed event micro-dollar amounts and Claude's final native total agree. Timestamp-less native cost-state duration is not a wall clock, so Hive correctly withholds a host comparison with `cost_state_timestamp_unavailable`. |
| Gap disclosure | Public fixtures verify positive host/transcript lower bounds and stale/incomparable host totals. The historical `1adb320e` transcript is unavailable here; its exact live gap was not revalidated. |
| Resume | Real disposable session `fe3acbf5` resumed twice, retaining one process start and cumulative totals `$0.0200000`, `$0.0241676`, `$0.0287112`; Hive never adds those snapshots. Three requests and 17 exported events gave full coverage and exact `$0.0287112`. |
| Nested subagents | Real outer `a23026375247b515d` has depth 1 and parent main; inner `a217214107a01738f` has depth 2 and that outer parent. Their exact amounts are `$0.0200234` and `$0.0041930`. |
| Fast/US modifiers | A disposable request launched with process-only `fastMode: true` succeeded, but its usage recorded `speed: standard`; fast live pricing remains unverified. US-only inference was not enabled. All supported rate/modifier combinations pass fixtures; this does not substitute for live modifier evidence. |
| Tool consistency | Every inspected real session has zero `unallocated_usd`; the healthy nested run reports two suspected prefix rewrites. Nine public allocation journeys cover 150 randomized requests/bands, exact remainder order, reset/expiry behavior, byte-only storage, oversized fragments, streaming updates, inode replay and migration equivalence. |
| Resident continuity | A real `telemetry watch` process collected a disposable mixed Codex/Claude bead set from schema 8. While its next old-source sweep was held in flight, the isolated source checkout advanced to the promoted schema-9 implementation. The same process then migrated and collected both hosts: counts changed from one to two each, three batches completed, its secret/listener remained usable, and it terminated cleanly without a restart. No batch failed. The source-update witness is test instrumentation in the disposable clone only. |
| Codex reread | After that migration, `--from-start` retained two Codex responses, zero gaps and exact `$0.003000000000`; no duplicate charge appeared. |
| Every subagent file | All two `agent-*.jsonl` files from the live nested run were collected, with zero remaining bytes and source errors. The historical `f8e5bf9f` example is absent and its quoted `$2.19883` was not revalidated. |
| Event source | The real nested run exported 19 events, all accepted, with matching session/request IDs, full coverage, no sequence gaps or token mismatches, and zero event-only requests. Only allow-listed fields survive ingestion. CLI process settings reached the exporter; desktop managed-settings overrides and `/clear` were not exercised. |
| Receiver outage | After a successful nested run, one real resumed request ran with the listener absent, then another with it restored. Seven transcript requests totaled `$0.1090742`; event coverage fell to 6/7, exactly one row was transcript-only, and `complete_estimate_usd` became null with `incomplete_event_coverage`. No sequence gap is required when a whole process export is lost. |
| Detail rows | All seven detail rows sum exactly to the transcript total; event-only detail sum is zero. The original five joined rows sum to `$0.069755` using either transcript prices or event host amounts. Keyset paging, tier selection and contention are covered by public fixtures. |
| Bead attribution | Five native cost journeys and five history journeys cover mixed-host handoff, close/reopen, unowned work, equal shared claims/remainders, near-boundary requests, missing-middle history, explicit null assignee, assigned creation, rename/prefix cascades, delete/reset, contention, 1,002-row paging, schema recovery and the repeated DST hour. Reconciliation balances exactly. The configured original live Beads server was not listening and its current data directory was unavailable; no replacement store or stale backup was substituted, so exact `hv-4up`/`hv-67e` intervals remain unverified. |
| Tool-split accuracy | Three real sessions (`4d0cc2b3`, `fe3acbf5`, `c7f89d5c`) report `byte_split_error: null`, because none has 20 usable single-part segments. They respectively contain 1, 3 and 7 requests and all reconcile exactly. The 21-single-part fixture establishes the measurement path. Empirical accuracy is **not accepted** from insufficient samples; tool attribution remains explicitly estimated. |

The original completed-session example `4dd72542` is also absent; the live arithmetic checks above are substitutes, not claims about that historical session. Claude fast mode supports a process-scoped settings override, while US-only app inference is an organization-level option; these were checked against [official fast-mode documentation](https://code.claude.com/docs/en/fast-mode) and [official US-only inference guidance](https://support.claude.com/en/articles/15422948-enable-us-only-inference-for-your-organization). No organization setting was changed to manufacture an acceptance result.

Cold reviews found concrete bugs before promotion, including renamed/deleted history preservation, transient status recovery, streamed part growth, replayed oversized bytes and migrated early content blocks. Each confirmed defect gained a regression. Bucket equality alone missed several wrong allocations, so it is recorded only as bookkeeping evidence. User-directed autonomous implementation does not turn the remaining live-environment and accuracy checks into passing evidence.

## Historical Claude acceptance recovery, 2026-09-25

The original local transcripts became available and were collected into a private store without activating a production collector or changing Claude settings. This exposed a native streaming shape missed by the earlier fixtures: subagent partial usage omits `speed`, while the final record supplies it. The hv-b4r fix enriches absent modifiers, preserves known modifiers during rereads, and continues rejecting conflicting known values; schema 12 replays existing Claude sources to recover rejected finals.

[Measurements](measurements/claude-historical-20260925.json) contain only IDs, counters and cost evidence:

| Session | Collected files | Priced / observed requests | Exact priced USD | Evidence |
| --- | --- | --- | --- | --- |
| `4dd72542` | main + 1 subagent | 22 / 22 | 0.890536800000 | Matches the original native final cost total exactly. |
| `1adb320e` | main + 1 subagent | 42 / 43 | 1.885827200000 | One unfinished subagent request still lacks a speed; it remains unpriced. |
| `f8e5bf9f` | main + 2 subagents | 60 / 60 | 2.198825200000 | Matches the plan's rounded $2.19883 and includes every agent file. |

All three have zero parse gaps, zero remaining bytes, zero unallocated USD, and request-detail sums equal to the priced totals. The three reports count 20, 40 and 48 suspected prefix rewrites respectively; these are heuristic markers, not confirmed cache waste. Every `byte_split_error` remains null (fewer than 20 qualifying single-part segments), so empirical tool-split accuracy is still unaccepted. The native cost-state records contain no timestamp; `host_reported` and `unrecorded_usd_lower_bound` correctly remain null with `cost_state_timestamp_unavailable`, so the required live positive-gap report is still not demonstrated. No missing timestamp or modifier was invented to meet acceptance. The desktop managed-settings and `/clear` checks from the earlier acceptance record also remain outstanding. These limitations keep the dashboard's hard prerequisite gate open.

The same private store read live Beads history without changing native tasks. [Interval measurements](measurements/live-intervals-20260925.json) confirm `hv-4up`'s Codex interval `00:07:49.580Z–03:12:35.924Z` and Claude interval `03:12:35.924Z–03:16:14.268Z`, and `hv-67e`'s interval `04:08:16.899Z–04:16:03.590Z`. Catch-up required 29 and 7 bounded report attempts respectively. A subsequent 30 attempts for `hv-0m1` did not finish history refresh: its cached interval is present, but attribution correctly remains unknown. The final reconciliation balances $26.342745 solely in the unattributable bucket while behind; this is **not** successful live attribution acceptance. A reliable steady-state catch-up/performance check remains outstanding for `hv-qg7`.

## Native label history recovery (hv-xi7), 2026-09-25

The live replay failure above was caused by `label_added`/`label_removed`: 310 ordinary label events made 120 histories unsupported. The fix recognizes these as non-ownership events, validates their timestamps, and still rejects unknown event kinds or a label carrying ownership fields. The disposable-store regression executes native label commands and inserts the historical event shapes because current bd 1.2.2 does not emit label history for those commands; it covers assigned creation, changes during ownership, changes after close, and corruption rejection.

[Live recovery measurements](measurements/live-label-recovery-20260925.json) show all 173 cached histories known after normal bounded refreshes, with no reset or deletion of history. Recovery took 13 calls; the following `hv-67e` and `hv-0m1` reads each caught up in one call (1.27 s and 0.49 s). `hv-4up` retains both expected intervals and $3.9700224; `hv-67e` has $2.7772584; `hv-0m1` has $8.5364018. Reconciliation now balances $26.342745 as $17.2344444 attributed plus $9.1083006 unowned, with zero unattributable dollars. One unfinished Claude request remains unpriced and Codex transcript coverage flags remain visible; these totals describe the collected private snapshot, not all live spending. This resolves the label-specific steady-state failure recorded above, while the outstanding positive live-gap, desktop event-source and empirical split-accuracy acceptance requirements remain open.

## Tollgate completion status, 2026-09-23

Provider candidate `01a0cead-dfd8-73a2-9b0f-6976155138f0` passed validation and was explicitly approved. Tollgate reported `promoted`, remote `synchronized` and cleanup `completed`, with certificate `01a0ceae-7bdd-7b82-8370-6ab766a232d6`. The certified release `d6f14f827772937d05eb6d9c1faf3876762fffef` was built in a detached worktree, code signed, installed and restarted. The installed app and CLI binaries matched the certified build. Native candidate status from the running app reported `local_master.status=synchronized`, `contains_tested=true`, `policy_enabled=true` and local master commit `d6f14f827772937d05eb6d9c1faf3876762fffef`. `tg --no-launch doctor` with Tollgate's repository ID returned `healthy=true` with all checks healthy.

## Manual backup and restore, 2026-09-23

Two disposable Beads 1.2.2 server-mode stores on Dolt 2.2.0 were used. `bd backup init <isolated path> --json`, `bd backup sync --json`, then `bd backup restore <isolated path> --force --json` in the second initialized store succeeded. The round trip took 6.08 seconds. Native `list --all --limit 0 --json` agreed for two beads on title, description, acceptance criteria, priority, flat metadata, historical assignee, status and the dependency edge. Native `comments <id> --json` agreed for the recorded note. The first bead carried `hive_resolution=completed` and a closed historical assignee. The disposable IDs were `hv-bf1` and `hv-6h8`. No production store was touched.

## Native dependent executor journey, 2026-09-23

In a disposable registered repository with a local bare remote, isolated Beads server and actual Codex thread `01a0ce84-c2c9-7d23-a621-b3bd114675f0`, the native `bd` commands filed `hv-hy3` and dependent `hv-xxo`. Both stored that thread as `hive_origin_thread`; the second initially had no `hive_project` and was explicitly corrected before work. `bd ready --json` showed only `hv-hy3`. Native `bd --actor <actual-thread-id> update ID --claim` set the assignee on each bead in order. Each change was made in a Tollgate-created worktree, cold-reviewed by a fresh subagent, committed with a Conventional Commit and submitted with `tg candidate HEAD --json`. `tg approve ID --wait --json` returned terminal `promoted`, `remote_state=synchronized`, `cleanup_state=completed` for candidates `01a0ce9b-6968-72e1-a1a4-4eeec469a428` and `01a0ce9d-5670-7013-8a7c-75a0badef7e7`. Tested commits were `f9d7362afde790962be5378ec3b6b719ca6a03e0` and `8579238cec0fdf1a0ac55ab929b8a676afba2374`. The first was closed with `hive_resolution=completed` before `bd ready` exposed the second; the second was closed the same way after delivery. `git rev-parse master refs/remotes/origin/master` and `git ls-remote origin refs/heads/master` all returned the second tested commit. The native journey took about 3 minutes 15 seconds from filing to second delivery, including agent review and model/tool time; individual native commands completed in less than a second. Beads 1.2.2, Tollgate 0.1.0 and Dolt 2.2.0 were used. The setup was disposable and no production task or remote was touched.

The first journey used one real executor thread. A separate disposable native claim race with two distinct real Codex threads is recorded below. A third candidate (`01a0ce9f-1063-72e0-b51f-85b74a1f8ec8`) completed the one-time real 30-minute native wait and source-update check.

The command log reconstructs at least 19 task-path native Beads/Tollgate invocations across the two deliveries. The overlapping Codex transcript window recorded 22 model response records and 21 tool calls, including provider investigation outside this journey. These counts and wall time describe one observed workflow, not a matched performance comparison. In a separate empty disposable state, the first `hive source` call selected the committed source in 0.159 seconds; two later calls took 0.0533 and 0.0527 seconds. The observed thread-level API-equivalent cost and coverage are below; there is no per-bead allocation.

A later native `bd update hv-hy3 --title 'First disposable delivery (renamed)'` succeeded on the closed first bead and `bd show --json` confirmed the new title with status still closed. This verified direct rename, although it was performed after the second delivery rather than between the two claims.

## Ordered repeat of the short journey, 2026-09-23

To close the ordering gap above, the same disposable registered repository and Beads store filed `hv-ctv` and dependent `hv-e0u` with the actual creator thread ID. The dependent initially lacked `hive_project`; native `bd dep add hv-e0u hv-ctv` established the edge before that key was added. `bd ready --json` exposed only `hv-ctv`. The actual thread claimed it, created a Tollgate worktree and delivered `ordered-first.txt` while removing the completed long-wait marker. Fresh cold review and `./ci` passed. Candidate `01a0cebe-bff9-7880-b492-eace6cb143c7` promoted tested commit `f2c7ce8280a5be89c12619f6c2de0c86ec506ae0` with synchronized remote and completed cleanup. The executor recorded `hive_resolution=completed` and closed `hv-ctv`, then renamed the **bead title** with native `bd update --title`. Only after that did `bd ready --json` expose `hv-e0u`; the same actual thread claimed it. A second Tollgate-created worktree added `ordered-second.txt`; fresh cold review and `./ci` passed. Candidate `01a0cebf-9e5e-7e13-a2db-d62ff0eca983` promoted tested commit `a376ef8d78ddb2ca0c5f0eabb6b51497aed6d23e` with synchronized remote and completed cleanup, and `hv-e0u` closed with `hive_resolution=completed`. Native `bd show hv-ctv hv-e0u --json` retained creator, assignee, prerequisite and the renamed closed bead title. Local `master`, remote tracking ref and bare remote `master` all resolved to the second tested commit. This repeat took about 2 minutes 19 seconds from filing to final closure, including fresh review and model/tool time. The command record contains 20 task-path native Beads/Tollgate invocations; the same transcript interval (14:48:55–14:51:15 UTC) contains 21 model response records and 20 tool calls. Some model/tool activity in that interval was unrelated provider investigation. Beads 1.2.2, Tollgate 0.1.0 and Dolt 2.2.0 were used.

The workflow's required **Codex task title** transition was then exercised as an adjacent dependent pair: after `hv-e0u` was delivered and closed, native `set_thread_title` renamed actual thread `01a0ce84-c2c9-7d23-a621-b3bd114675f0` to `⚒️ Hive acceptance: next delivery`. Only afterward did it file dependent bead `hv-nrh`, inspect readiness, claim as that same thread, and deliver `title-followup.txt` through a Tollgate-created worktree and fresh cold review. Candidate `01a0cec3-351d-7213-ade2-81f496ad649e` promoted tested commit `89d01e044634da698818919baa2eaeada414a79a` with synchronized remote and completed cleanup; `hv-nrh` closed with `hive_resolution=completed`. This verifies direct native Codex title change before the next dependent claim and delivery. The first two-bead pair's bead-title rename alone did not test that UI action.

After the repeat and title follow-up, bounded observation through cursor byte 9,554,943 found 412 native unique response IDs in a separate read-only transcript scan and 412 derived response rows. The thread-level cost report showed 412 priced responses, zero unpriced or missing usage, standard-tier API-equivalent estimate $12.3752176, two disclosed parse gaps, no remaining bytes at that scan, fresh associations for six disposable beads, and one total for the shared thread. This is the full linked thread's observed cost, including unrelated work; it is not a per-journey or per-bead allocation.

## Local gate budgets, 2026-09-23

Three consecutive quiescent runs after the routing fix passed. Wall times from `/usr/bin/time -p` were 9.30, 9.89 and 8.86 seconds for `scripts/check-fast` (30-second budget), and 17.07, 17.10 and 17.35 seconds for `scripts/check` (120-second budget). The full gate includes native Beads server tests; hosted CI is asynchronous.

## Native observation and manual archive check, 2026-09-23

An isolated Hive source clone at `13830f86828cd18e43c3935d1e855076d5f94399` and isolated state read the disposable Beads store through `hive telemetry links --json`. It returned four links: creator and executor for each of the two closed beads, all pointing to the same real Codex thread. It reported no link gaps. `hive telemetry sweep --native-index /Users/dthurn/.codex/state_5.sqlite --json` collected the known thread in bounded 1 MiB batches until its cursor reached the then-current end. A separate read-only JSONL scan through the exact stored byte cursor counted 233 native `token_usage_record` records with 233 unique response IDs; the telemetry database had 233 responses. `hive cost --task <thread-id> --json` showed one thread total with all four bead relations, 233 priced responses, standard tier assumption, observed model `gpt-6-sol`, and an API-equivalent estimate of $6.6413548 at that scan. It also disclosed one skipped oversized transcript line; no response was lost in the cursor-aligned comparison.

The opt-in `hive telemetry watch --native-index ... --interval-seconds 5` ran twice under the disposable bootstrap configuration. Each run emitted one `CollectorBatchFinished`, accepted SIGTERM and exited 0; no host service was installed. A second cursor-aligned read-only scan after restart counted 236 unique native response IDs and the derived store had 236 rows and 236 distinct response IDs. Native Beads work was unaffected. The Codex task list identified the linked thread as **active**, so the manual archivist correctly skipped it. No eligible disposable thread was archived; that capability remains unproven until native inactivity evidence exists.

## Competing real-thread claim, 2026-09-23

The same isolated Beads store filed disposable bead `hv-gty` with the actual creator thread ID. The executor thread `01a0ce84-c2c9-7d23-a621-b3bd114675f0` and a fresh review thread `01a0ceae-d81c-74c0-8f60-ef2994d201ca` each waited on one filesystem marker, then both ran native `bd --actor <own-thread-id> update hv-gty --claim`. The executor's command succeeded (exit 0). The review thread's command returned exit 1, explicitly saying the issue was already claimed by the executor. Its read-only `bd show hv-gty --json` confirmed `in_progress` and the executor assignee; the losing thread made no task or file edits. The test bead was closed with a recorded completion resolution. This establishes the native one-owner primitive for two real Codex actors; it does not impose a Hive-wide capacity limit.

## Native wait and source update, 2026-09-23

Disposable candidate `01a0ce9f-1063-72e0-b51f-85b74a1f8ec8` tested commit `b6dc653d0b4c71d16b0fec6806c08615f401e20b` with a native CI command that sleeps 1,800 seconds when `long_wait.txt` exists. The actual Codex tool host started `tg approve ID --wait --json` at 2026-09-23 14:15:29.894 UTC, retained its process session, and retrieved terminal output at 14:45:48.994 UTC: 30 minutes 19.100 seconds of host-observed wall time. No model-driven candidate polls occurred during that interval. The isolated Hive source clone initially selected `13830f86828cd18e43c3935d1e855076d5f94399`. While the native wait was pending, `git fetch origin master` and `git merge --ff-only origin/master` advanced only that clone to `d330adb3a9c1c464541d5a78820fd6730644e604`, and a new `hive source` call selected the new commit. A retained opt-in collector process then emitted 14 successful batches selected from `d330adb3a9c1c464541d5a78820fd6730644e604`; after the same source clone advanced to `329cafe755da01124f930d19fea6e68dc3cddec1`, that same process emitted four successful batches selected from the new commit before clean SIGTERM. Source consistency within one running call is covered separately by the source-selection black-box test in `scripts/check`. The native wait returned `promoted`, `remote_state=synchronized`, and `cleanup_state=completed` after provider-reported `elapsed_ms=1800728` (30 minutes and 0.728 seconds of validation). Exact tested commit `b6dc653d0b4c71d16b0fec6806c08615f401e20b` was present at local `master`, `refs/remotes/origin/master`, and the bare remote's `master`.

## Direct native task routing (hv-4up)

On 2026-09-23, Beads 1.2.2 ran the `skills/shared/routing.md` prefix against a disposable server/store from an unrelated non-Git cwd with ambient `BEADS_*`, `BD_*` and `DOLT_*` variables removed. Create, claim with the actual invoking Claude Code session ID, close and list succeeded. After the server stopped, listing failed with auto-start disabled and no store appeared in the cwd. Against the live store, the same `BEADS_DIR` selection from a Tollgate worktree reported the configured `beads_dir`, database and server through `bd context --json`; context needs a Git cwd. No production configuration or Fulcrum state changed.

The recipe does not guard against conflicting ambient routing variables; skills instruct agents not to rely on them. The former task launcher and its override-rejection tests are retired. The focused observer routing test exercises read-only observation through the public Hive command with conflicting environment, invalid metadata and server outage. Source selection and resident continuity code are unchanged.

## Hive dashboard, 2026-09-25

Implemented plan revision 5, steps 1–8, for epic `hv-762`. The user authorized uninterrupted execution and selected Hive and Battlement observation from `2026-09-24`; no intermediate sign-off is pending. Collection and the separate loopback dashboard are enabled locally. The following distinguishes measured live data from disposable protocol/host fixtures.

| Plan check | Evidence and outcome |
| --- | --- |
| 1. Identity | A SQLite backup of the live store, reconciled through the native cost command and then allowed to finish summary publication, contains 230 cards totaling **656.884386600000 USD** with **zero picodollar difference** from the oracle; the seven-day strip independently equals request rows in its window, with two requests explicitly unpriced. [Measurement](measurements/dashboard-identity-20260925.json). Using a stable backup avoids comparing different instants while both projects are active. |
| 2. Known bead | `hv-4up` totals **3.970022400000 USD**, with `01a0d0bb-8916-7380-8a5e-24cacfaaeda1` and `1adb320e-554a-4b4e-a560-cbbeb03a8bdf` ownership intervals visible in the browser; all eight dimensions sum exactly. |
| 3. Project sessions | Native-index/transcript journeys cover the observation cutoff, repository membership, Tollgate and external Codex worktrees, sibling-path exclusion, root folding and a child with its own claim. An activity-first regression advances a changed session ahead of 100 idle sessions. Real Hive/Battlement cards are visible; no extra user conversation was created merely to manufacture a live acceptance session. |
| 4. Roles | Browser data shows Executor, Weaver, Ad hoc, Warden and Archivist where native evidence exists; component tests verify five distinct emblems, and transcript tests verify a warden child inside an executor. Encrypted Codex spawn prompts can leave a reviewer role inherited; the UI labels inheritance and does not infer a role from this author's intent. |
| 5. Diagnostics | Disposable native-shaped transcripts cover failing tools, slow tools, retries, intentional sleep, AskUserQuestion waits, permission denial, interruption, compaction and cache events. The live known-bead error marker opens a verified source excerpt in the browser. Permission-denial events are visible when exported; the duration of a UI-only permission prompt is not guaranteed to appear in native transcripts, so no elapsed permission wait is invented. No follow-up question was sent for testing. |
| 6. CI | Candidate observation fixtures retain a failed and then passing attempt, step outcomes and branch-only provenance. UI journeys render both attempts and the branch label; API tests verify the exact observed buildset and step are passed to `tg logs`, retain only a 4 KiB excerpt in memory, and reject an unobserved attempt. Native CLI inspection verified plain log output and exact-buildset support; the browser opened the actual `hv-l85` CI attempt and displayed its final test output. |
| 7. Hot reload | With a real React build and the same preview server PID **33454**, a UI commit produced **New version — reload**, reload built the new tree, a Python-only commit retained the same UI tree without a pill, and a deliberate TypeScript syntax failure served the previous bundle with a visible warning. These commits were confined to `/tmp/hv-dashboard-preview/repository`, not promoted to Hive. Black-box server tests additionally cover concurrent first loads, retry/backoff, source immutability and shutdown cleanup. [Python-only identity](measurements/dashboard-python-reload-20260925.json). |
| 8. Security | Assembled HTTP probes reject foreign Host and cross-site metadata with 403, traversal and invalid IDs with 404, and match ETags with 304. Native server tests cover POST, no CORS, symlinks and rejection before builds. Hostile Markdown component tests exclude scripts, raw HTML, remote images and executable links. |
| 9. Performance | Assembled source-selected HTTP on the live-store copy: feed **124.83 ms**, cold known-bead detail including the native panel **549.72 ms**, request page **80.35 ms**. [Measurement](measurements/dashboard-http-20260925.json). Five API journeys include 7,000-card scale, exact large amounts, ranges and read-only access beside a writer. Collector stress tests verify bounded progress and publication; initial historical catch-up can require more than two sweeps and is labelled in the UI. |
| 10. Visual review | Static mockups preceded React implementation. Accessibility snapshots and screenshots were reviewed at **1440, 1024 and 600 px** against Fulcrum's primitive, component and feed references. Checked feed filters, bead and session navigation, timeline/diagnostic source reads, breakdown selection, responsive layouts and safe content; no open visual defect remains. Narrow charts and tables scroll locally rather than widening the page. |

The previous cost-plan limitations still apply: empirical tool-byte allocation error, complete Desktop managed-settings coverage and `/clear` host evidence are not established by dashboard delivery. Amounts remain API-equivalent estimates; incomplete coverage is marked `≥`, unpriced requests remain separate, and estimated tool allocation is labelled. Old or failed delivery candidates can leave a closed bead in Needs attention until a later observed success resolves that signal. Build and route changes are distinct: ordinary Python/UI source updates reload without restarting, while this initial UI release adds resident request-query routes and therefore receives one transport restart on activation.

## Dashboard redesign: shared shell checkpoint (hv-0gi.1)

The [scoped shell review](measurements/dashboard-redesign/shell/README.md) records
natural, wide, tall and narrow renders and both sides of the existing breakpoints,
with zero horizontal overflow after grid and role-summary repairs. Seventeen
frontend journeys cover existing evidence views plus direct-route fallback,
reload and independent history-entry restoration. Cold review found and verified
the same-query cache correction. The remaining redesign children and the full
glyph, border-stack, contrast, state and interaction matrix are unfinished; this
checkpoint does not supersede those requirements or claim whole-plan acceptance.
