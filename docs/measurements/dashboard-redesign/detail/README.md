# Detail workspace smoke review — hv-0gi.3

Inspected real task hv-0q1 on the assembled preview, starting at the natural
1280 × 720 viewport. Final geometry covers 1600 × 1000, 1440 × 900, 1000 × 1100,
390 × 844 and both sides of 1100, 900, 768 and 639 px breakpoints. All thirteen
measurements show zero page horizontal overflow. Wide, target, tall and narrow
screenshots retain the overview. The source manifest pins file hashes and base;
the native bead records the certified source/integration commit.

The main/supporting overview uses a 2:1 split above 1100 px, following the text
contract. Long real task titles wrap fully on narrow windows. Default evidence is
reduced to lifetime spend, the existing timeline, recent observed delivery
milestones, truthful lifetime candidate counts and role amounts. Explicit task,
whole-session, unowned-tail and ledger scope stays visible near identity. Cold
review caught the missing whole-session qualifier; fixtures now cover both session
scopes plus truthful zero/one/multiple session actions.

Live Delivery inspection showed five retained candidates and loaded the exact
resource-report certification attempt's log tail. The excerpt panel gained focus
near the tabs; closing returned focus to that Log tail button. Source reads remain
on demand. Diagnostics exposed the existing kind counts and a selected Tool error
filter; narrow diagnostics stayed contained. Component journeys additionally cover
reloadable tab/range/section state, strict API parameter whitelisting, hidden range
counts, cached native records and commands, unavailable logs, unknown-time events,
source cancellation on task changes, request pagination/sort/range resets and all
previous navigation/accounting tests. Thirty-three frontend tests pass.

At 1440 × 900 the existing chart is 220 px high, but its expanded legend/range
controls place Recent activity at y≈928, below the viewport. Compact line/bin and
range-control presentation remains explicitly required by hv-0gi.4. The current
SVG's small scaled labels also remain part of that next step; they are not claimed
as final visual acceptance. Full glyph/border-stack/contrast/state/zoom checks and
integrated journeys remain hv-0gi.5. This scoped review is not a whole-plan verdict.

One initial live read returned the backend's generic migration/recovery error;
the actual store and source both reported schema 19 and the native retry recovered.
No migration, database write or service restart was performed for verification.
Cold review, fast/full certification and promoted hot reload are recorded natively.
