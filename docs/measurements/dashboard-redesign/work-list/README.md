# Work-list smoke review — hv-0gi.2

The read-only assembled preview was inspected at its natural 1280 × 720 viewport,
then at 1600 × 1000, 1000 × 1100, 390 × 844 and both sides of the remaining
1100, 900, 768 and 639 px breakpoints. All 12 final geometry entries show zero
page horizontal overflow. `source.json` pins the exact source hashes and base;
the native bead records the certified integration commit. Screenshots retain the
wide, tall and narrow default state and a narrow open role disclosure.

The former card grid, decorative issue banner and three-role preview are removed.
At 1600 px, content begins at x=256, the work header at y=415 and rows at y=438;
columns align across every row. Title width is approximately half the list and
amounts align right. These landmarks follow the approved text contract rather
than generated sample identities/counts. All stored title text wraps without
clamping; no tooltip-only full-title access is required. At 390 px, each row
stacks title, project/state and amount; a visible caption retains lifetime scope.

Live interaction checked project selection, another role filter, applied count,
reset and Escape focus return. A query-change defect initially unmounted the
filter disclosure; retaining its component through loading fixes that regression
and preserves the current project option. The final 22-test suite also covers
this behavior, all five kinds and thirteen states, large exact amounts, partial
zero, all roles, all affected-work links, coverage deduplication, stale/empty/error
states, coherent page refresh and the shell's navigation/history journeys.

The live role disclosure rendered all nine observed roles and stayed within a
358 px content region in the narrow viewport. Coverage findings and their links
remain in the coverage disclosure; other ranked findings remain in Issues.
Default rows omit IDs, descriptions, emblems and badge clusters. Source data,
accounting arithmetic, feed ordering and API requests are unchanged.

This is the work-list child's scoped smoke evidence. Tight glyph bounds, complete
numeric border stacks, contrast/zoom/state stress and integrated detail journeys
remain required by hv-0gi.5. No whole-redesign satisfied verdict is implied.
Cold review, final fast/full checks and resident hot reload are recorded natively.
