# Implementation and acceptance status

## Temporary justiciar titles (hv-72z)

Justiciar entry and shared recovery/deferral guidance now require 🔥 on every invocation for the duration of justiciar powers, preserving the bead and summary and restoring the appropriate title afterward. Delegated reviews mark the invoking task and any independently renameable reviewer task; shared-identity subagents leave parent renames to the owner. This is skill-level guidance, not host-enforced automation. Structural validation, cold review and full Tollgate delivery are recorded on the native bead before closure; a live justiciar title lifecycle has not been observed for this change.

The former Fulcrum design and Hive admission/session/delivery assertions are historical. Their deletion retires those requirements; it does not count as implementing them.

## Implemented in this branch

- Direct native Beads task commands, with shared skill instructions selecting the configured store through `BEADS_DIR` and disabling auto-start and auto-push. The former task launcher and argument-override parser are removed; task routing is an agent convention.
- Explicit configured server validation and environment sanitization remain for read-only bead observation.
- Source-selected local-master calls and immutable running-call snapshots.
- Bead-linked creator/assignee worklist including closed work, cached discovery across outages, and thread-level incremental usage/cost associations.
- Telemetry reports read without the database write lock; contention within the 0.1 s SQLite timeout is a structured `Busy` error, and cost defers estimate retention (reported as `unretained_estimates`; a later report may then reprice after a catalog edit) and link-cache refresh under contention. Covered by black-box lock tests; not yet observed against a live collector.
- Host-neutral thread and agent identities with compatibility aliases, schema version 9, and atomic collector-led migration of existing Codex usage, cursor, gap, link and quote evidence; reads of older stores ask for collection, and newer stores are rejected without writes. Migration/reread, rollback and version-guard tests run in both gates; the disposable mixed-host watcher passed an in-flight schema 8→9 source update without restarting.
- Native role instructions, simple skill links, reduced fast/full gates, and manual operations guidance.
- Executor and shared guidance make original-scope transitions explicit, require epic-context inspection and discoverable coordination records, and distinguish same-project workstreams from authorized cross-project execution; these are instruction-level conventions, with no observed end-to-end concurrent-epic acceptance.
- Weaver requires a parent implementation epic and child implementation beads; native deferral for initial approval holds requires fresh independent justiciar agreement. This is instruction-level guidance, without an observed end-to-end Weaver journey.

## Acceptance evidence and remaining limits

- Direct native task routing passed a disposable create/actual-thread claim/close/outage probe; it relies on agents not setting conflicting ambient routing variables. See `docs/acceptance.md`.
- Tollgate's native completion output was checked against a disposable two-candidate journey. Provider candidate `01a0cead-dfd8-73a2-9b0f-6976155138f0` corrected its missing local-master distinction, passed validation, was explicitly approved and promoted, and was installed from certified release `d6f14f827772937d05eb6d9c1faf3876762fffef`. After restart, native status reported `local_master.status=synchronized`, `contains_tested=true` and `policy_enabled=true` for that candidate; `tg --no-launch doctor` passed for the Tollgate repository.
- The disposable two-bead executor journey, distinct real-thread competing claim, native backup/restore round trip, real linked-thread observation comparison, collector restart, conservative active-thread archive skip and one-time 30-minute native wait/source update passed. No eligible disposable thread has been archived. See `docs/acceptance.md` for evidence and limits.
- Manual remote sync was exercised once on 2026-09-24 against the live store: `bd dolt push` to a private GitHub Git remote, then a fresh `dolt clone` of that remote contained 26 issues, matching the live `bd list --all` count; contents, dependencies and comments were not compared. It replaced the retired Fulcrum brain on that remote. There is no scheduled sync, and pulling onto a second machine is untested.
- Three quiescent runs passed the 30-second fast and 120-second full budgets; timings are in `docs/acceptance.md`.
- The opt-in collector command and stop/restart behavior were validated with disposable configuration; no host service was installed. Production activation and Fulcrum work migration remain separate operator actions.
- Claude Code workflow compatibility is instruction-level only: skills name `CLAUDE_CODE_SESSION_ID`, desktop session title/archive tools and `install-skills --agent claude`. No Claude Code executor journey, claim, rename or archive has been accepted. Roles invoked without a project resolve it from the session's starting directory, or a linked worktree's main checkout; this is instruction-only and has not been observed across projects. An Agent-tool subagent was observed to inherit its parent's `CLAUDE_CODE_SESSION_ID`, so subagents cannot be independent executors. A desktop-app session started from a manual routine, with no click, claimed `hv-0yu` under its own session ID, renamed itself `⚒️`/`✅ [hv-0yu] …`, and delivered and closed it; the bead skill instead offers a `spawn_task` chip the user approves, which has not been exercised end to end. Codex `create_thread` handoff is unverified. Archive needs a mapping from the stored session ID to the desktop app's session ID. Telemetry now probes Codex and Claude sources without UUID-version routing, collects Claude main/subagent files incrementally, and preserves monotonic partial-output updates by request identity. `claude_projects` is explicit bootstrap routing and defaults to `~/.claude/projects`; ambient `CLAUDE_CONFIG_DIR` is not used. Both-host matches, duplicate Claude project matches and uncached Codex-index outages stay uncollected with visible errors. Fixture commands and sweeps cover identity validation, duplicate requests, malformed counters, old cache TTLs, unsupported iterations and late subagents; a disposable native watcher later passed mixed Codex/Claude collection across a schema 8→9 source update with an old sweep in flight and unchanged listener. Claude pricing now uses exact per-model rates, observed speed/tier/geo modifiers, both cache-write TTLs and separate web-search fees. Unknown models/modifiers and unsupported iterations remain unpriced, explicit Claude `--tier` is rejected, and coverage says transcript-only totals are lower bounds. Stored rate evidence survives source edits and partial-output completion (including late retention); fixture arithmetic covers every listed model and modifier combination. Main-file `cost-state` records now retain one latest total per process start without summing segments; reports disclose a host total and dollar gap only when a single process covers all observed request times and the difference is nonnegative. Fixture checks cover rereads, resumed processes, malformed records, stale subagent coverage, timestamp offsets and unknown-model host totals; live Claude Code 2.1.282 resumes twice retained one process start and cumulative totals without summation. `request_detail` exposes retained per-category decimal amounts for both hosts; `hive cost --requests` uses 500-row keyset pages with query-bound cursors, selects one Codex tier, and marks current-card estimates when contention prevents retention. Schema migration backfills components from retained rates without repricing; black-box checks cover paging, huge exact amounts, partial-output updates, migration and contention. `docs/claude-cost-plan.md` proposes Claude Code cost estimates, per-request detail and per-bead breakdowns, replacing this version heuristic with a two-host transcript probe; the full plan is approved for implementation. Steps 1–11 supply identity/migration, transcript and event collection, pricing, host comparisons, merged request detail, exact agent/skill partitions, historical-owner collection, per-bead attribution and estimated tool allocation; acceptance evidence and remaining environment-dependent checks are recorded in `docs/acceptance.md` (tracking beads `hv-b4r`, `hv-2uo`, `hv-qg7`, `hv-8ib`).

No assembled acceptance or production activation is implied by unit tests. Missing native activity evidence means archivist skips archival.

The opt-in `telemetry watch --otlp-port [PORT]` receiver (default 4319 when supplied without a value) accepts authenticated loopback OTLP/JSON log bodies and spools them unchanged, with private files, a 4 MiB body limit, eight connections, 5/30-second header/body deadlines and a 256 MiB / 65,536-file spool cap. Port failures are visible in collector status while the timer continues. `telemetry otlp-config` emits placeholder settings and writes the actual secret only to an explicitly chosen new file; it never changes Claude settings or metrics configuration. Loopback fixtures cover rejection codes, spool bytes/permissions, slow headers, saturation, bind failure and a live watcher across a committed source update. The resident receiver remains transport-only; current source-selected sweeps now ingest up to 16 MiB/two seconds, track every event sequence, discard non-allow-listed attributes, isolate bad records, join request IDs and retain unlinked events for seven days only after a successful Beads refresh. A disposable tool-free Claude Code 2.1.282 CLI request exported five events to loopback, including one matching API request; per-process settings were used and no persistent Claude settings were changed. The real captured shape is a sanitized regression fixture; its request joins at exact $0.02 with full sequence coverage and the host total. Main-file replay in schema 5 recovers timestamp-less `cost-state` totals; validated duration orders updates but cannot prove wall-clock coverage, so host comparison remains unavailable for that format. Event-only requests derive cache TTL/geo where the host micro-dollar figure permits, retain rate evidence, and remain separate from transcript totals. Missing sequences, unjoinable/rejected events, token disagreements, unpriced requests, partial collection and host discrepancies prevent a complete estimate. Two real CLI resumes succeeded; their running totals were $0.0241676 and $0.0287112 with the original startTime. All 17 exported events and three transcript requests then produced exact $0.0287112, full event coverage, no sequence gaps or token mismatches, and one host segment. A later real CLI nested-agent export passed; desktop managed-setting overrides and `/clear` remain unverified.

Claude thread reports partition transcript prices exactly by recorded agent and attributionSkill, including inline sidechains and missing skill values. A bounded transcript replay indexes spawning tool IDs; metadata joins recover nested parent agents, while missing or ambiguous identities stay unknown. Event-only totals are separate rows grouped by query source. Request details expose the same parent evidence, and manual collection refreshes metadata as sweeps do. Fixture commands cover child-first arrival, nested and inline agents, absent/deleted metadata, unpriced requests, ambiguous spawn IDs and schema-5 replay without repricing. The later live CLI nested-agent observation passed at depths 1 and 2 with the correct parent relationship and exact amounts; desktop-specific acceptance remains unverified.

Beads history now uses two fixed read-only SQL templates with validated UUIDv7/bead literals, the same explicit server routing, and a two-second budget shared with current-link refresh. Pages deduplicate event IDs and rewind ten minutes after catch-up; each event cross-checks UUIDv7 UTC time against server-local time with a one-hour DST tolerance. Historical assignees of both hosts remain linked, including invalid-time events; schema errors preserve cached links and disable retention. Status exposes caught-up/behind, errors and unknown beads. Real disposable bd 1.2.2 tests cover handoff, schema drift, 1,002-row cold start, late visibility, invalid IDs, explicit null assignees and the repeated DST hour. Native reopened events contain a new open status and retain the assignee. Step 10 adds ownership replay and deletion/rename reconciliation below.

Ownership replay checks each before-state and the final native bead snapshot, rebuilds mismatches, and treats persistent uncertainty as unattributable. `cost --bead` and `cost --reconcile` use one retained-price snapshot, including event-only records at their event timestamps. Equal shared claims distribute remainder picodollars in bead-ID order; creator links never receive allocations. Cached closed intervals survive delete and rename/prefix cascades. Native rename emits a `renamed` event that changes identity but not ownership; issue creation timestamps round to seconds, so uncertain intervals use UUIDv7 creation times. Operations now document maintenance hazards, cursor reset, and claim-before-work/new-follow-up-bead guidance. Five native cost scenarios pass, including unseen close before rename and cursor reset after deletion; five history scenarios cover paging, failure recovery and native clock checks. Dirty replay scheduling preserves the two-second Beads budget and resumes pending work across sweeps. The expanded native suites exhausted the former 120-second full gate after passing the new cost/history cases; its limit is now 180 seconds, while the fast gate stays at 30 seconds and collection deadlines stay unchanged. Tollgate certified and synchronized the implementation commit to remote master.


Tool allocation stores only prompt-visible part byte lengths, kinds and tool IDs, never prompt or result text. Independent agent chains use measured prompt growth, positional cache bands (1h before 5m), integer largest-remainder splits, measured output thinking and estimated carried thinking. Compaction, shrinking prompts and early cache misses reset allocation to rewritten context; cache expiry alone does not. Oversized skipped bytes remain weighted across read chunks. Thread buckets reconcile to priced transcript totals, while event-only charges stay separate; bead tool buckets partition each attributed share, including explicit Codex/event-only buckets. Delegated Agent spend is separate exact context, never added twice. Accuracy reports an in-sample single-part fit only with at least 20 samples, not proof of mixed-part accuracy. Nine public allocation scenarios pass, including 150 randomized requests, source-order remainders, compaction, rewrite/expiry, oversized chunks, migration and streamed-versus-final equivalence. Cold review found and verified fixes for stale streamed byte lengths, historical input replay after inode replacement, duplicate oversized fragments, late thinking measurements and distinct early blocks during migration. A real nested-agent CLI observation collected all two subagent files (depths 1 and 2 with the correct parent), five requests and 19 events at $0.069755 with full coverage, zero gaps/mismatches and zero unallocated amount; its two early cache misses are conservatively classified as suspected rewrites. Tollgate certified and synchronized the implementation commit to remote master.


All twelve Claude-cost delivery steps are implemented. The full step-11 gate passed in 127.337 seconds within the expanded 180-second budget. Final evidence is in `docs/acceptance.md` and `docs/measurements/claude-cost-20260924.json`. A deliberate real receiver outage reduced coverage to 6/7 and correctly withheld a complete estimate. An attempted process-local fast request returned observed standard speed; US-only inference and desktop `/clear` remain unverified. The original historical sessions and live Beads examples were unavailable here, so native disposable equivalents cover the behavior without claiming those exact records passed. All three inspected real sessions had too few single-part segments for `byte_split_error`; numerical reconciliation is accepted, but empirical allocation accuracy remains unestablished. No persistent Claude settings, organizational routing, production service activation or Beads data were changed.

The opt-in Codex executor stop check reads native assignments and project-ready candidates, supplies one corrective continuation and then warns on unresolved repetition. Categorized stops (with a recovery account for blockers/pressure), new user input and interruptions disarm it; only the exact generated continuation preserves opt-in. Native disposable tests cover unfinished acceptance, next-work selection, deferred/competing/cross-project exclusions, a drained queue, pause/input/interrupt handling, a concurrent interrupt during Beads reads, identity isolation, source reload and server outage. This is feedback only: eligibility and stop-reason evidence remain agent judgments, and Beads ownership is unchanged. The generated three-event host configuration still requires native trust review; no automatic production hook activation or live Codex continuation is implied by these tests. See `docs/executor-stop-check.md`.

## Event timestamp consistency (hv-k8r)

Event-only request detail and bead attribution use the OTLP `event.timestamp`, including hourly and near-boundary accounting; duration no longer shifts the observation backward. Schema 10 repairs existing event observations during a collector write without replaying transcripts or repricing retained quotes. A native handoff regression crosses an hour boundary with a nonzero request duration and checks the public detail, bead amounts, reconciliation, and migration preservation.

## Queryable request tool allocations (hv-82b)

Schema 11 adds `tool_allocation(response, ordinal, bucket, tool_use_id, tool_name, component, tokens, usd, method)`. Token counts are integers and USD values are exact decimal strings from retained quote rates; `component` uses the request-detail price names (`input`, `cache_read`, `cache_write_5m`, `cache_write_1h`, `output`, `server_tools`). Thinking is an output component in its own bucket. Server fees carry zero tokens. Event-only, Codex and unpriced requests have no allocation rows.

Allocation remains estimated; exact reconciliation is bookkeeping evidence, not proof of the byte split's accuracy. Collector batches and first price retention refresh rows atomically, replaying the affected thread's saved context facts so late streamed blocks also repair later carrying shares. Schema migration backfills retained prices without transcript access or new pricing. Query with a read-only SQLite connection and sum decimal strings in integer picodollars or decimal arithmetic, rather than SQLite floating-point `SUM(usd)`. Tests reconcile every component's tokens and dollars, including reset/oversized fixtures, streaming, rereads, fees, event exclusions, migration, and retained rates across a source price change.

## Streamed Claude modifier enrichment (hv-b4r acceptance)

Native subagent partial records can omit `speed` and only report it with final usage. Collection now fills absent speed, service tier and inference geography from later records without erasing known values during rereads; genuinely conflicting known values still create a gap. Schema 12 queues Claude sources for replay in the existing byte budget, preserving original cursors and conflict gaps until matching records replay successfully, recovering the previously rejected finals. Historical acceptance now reproduces the original $0.8905368 arithmetic and $2.1988252 two-subagent total; the positive live gap, empirical split accuracy and desktop event-source checks remain outstanding as detailed in `docs/acceptance.md`.

## Native label events preserve ownership (hv-xi7)

Beads `label_added` and `label_removed` events leave ownership unchanged. Replay skips these and rename events when finding the first ownership before-state, preserving assigned-creation behavior even with intervening labels. Label records that unexpectedly carry ownership fields, unsupported event kinds, and invalid timestamps still fail closed. Existing unknown histories are repaired through their normal bounded rebuilds; no event data or validation is discarded.

## Dashboard project observation (hv-ijd, step 1)

Bootstrap projects now accept an optional ISO `observe_since` and pass immutable project configuration into each selected application call. Discovery resolves Git common directories before separator-bounded repository paths, caches cwd decisions, incrementally pages native Codex updates with a ten-minute overlap, and inspects Claude main files through bounded cursors and directory/device/inode caches. Status exposes discovery backlog/errors and unresolved directories; discovery failure or backlog prevents event retention.

Codex spawned threads resolve through native parent metadata before registry membership, including older records without `thread_source`. Their observations, sources and model contexts fold under the root while retaining child identity; retained response quotes do not change. Child-owned intervals take precedence over root inheritance. Matching request IDs from different native producers remain identity gaps. Collection reasons distinguish links, historical owners, project sessions and spawned children. Changed files and new sessions precede at most four idle retries; transcript progress receives a reserved allowance while event ingestion or Beads history is behind.

Black-box disposable journeys cover Git and removed worktrees, path boundaries and date cutoffs, Claude project membership, observation disable/re-enable, late native parent evidence, migration of already-priced child data, native child claims, duplicate-producer conflicts, a changed session ahead of 100 idle sessions, and discovery-outage retention protection. Fresh cold review found and drove the configuration and identity regressions above. Dashboard diagnostics, server and UI remain subsequent steps; no production observation setting has been changed.

## Dashboard diagnostics (hv-ijd, step 2)

Schema 14 replays existing source cursors within normal collection budgets to add role spans, paired tool calls, command results and session events without copying transcript or command text. Source offsets include device and inode identity for later bounded excerpts. Roles follow the selected skill inventory; child roles inherit the parent's role at spawn until explicit native role evidence appears. Reports classify retries and project-relative slow calls and commands separately, exclude intentional waits from slow counts, and calculate expired-cache rewrite amounts only from retained quote rates. Unreadable exec output is `unparsed`, not a guessed success. Multiple script commands retain observed chunk results but do not guess a command-to-output mapping when native output does not prove execution order.

Disposable native-shaped journeys cover Claude permission/API/interrupt/compaction and cache events, Codex list-form exec chunks, slow work versus sleeps, retry loops, replay migration, inherited and explicit nested child roles, late parent discovery, replay-stable title fallback, repository-scoped waits, and absence of sentinel transcript text in the database. Fast validation now runs the native collection smoke from the Beads link suite; the complete link suite remains in full delivery validation. Live permission-prompt wait detection remains unverified, and asynchronous question delivery is not treated as user wait time.

## Dashboard Tollgate observation (hv-q9z, step 3)

Schema 15 records UUID mentions only inside recognized Tollgate calls, then confirms candidate identity and repository through `tg --json --no-launch`. Repository mapping uses configured project paths, with a five-minute refresh; unmatched projects have no CI panel. Promotion timestamps come from native `promotion.completed` history events when available, not from validation certificates. Native branch fallback requires an exact bead ID or its hyphen suffix. Candidate timestamps, attempt and step facts are validated, while commands, logs and diagnostic messages are discarded. Terminal observations stay cached through outages; failed or malformed polling is visible and retries preserve prior facts. Missing candidate IDs are retried less frequently and do not imply an unavailable provider. Polling receives at most the remaining 0.3-second collector allowance. Ownership joins reuse native request attribution at the first mention, including child-owned intervals.

Fake-CLI journeys verify native argv, timestamp validation, transcript proof, ownership and branch joins, terminal caching, and outage retention. Production observation is now enabled for both Hive and Battlement from `2026-09-24`, as selected by the user; the separate collector is running from the main checkout. The UI and dashboard HTTP service remain subsequent steps.

## Dashboard read API (hv-l85, step 4)

Schema 16 adds collector-owned card and request materializations, cached native bead list fields and CI joins. `hive dashboard api` exposes feed, bead, session, ledger, excerpt, CI-log and status responses from one read-only SQLite snapshot, tagged with the selected code commit and UI tree. Bead panels use bounded native reads with cached fallback and fully routed copyable commands. `cost --bead ID --requests` supplies the same exact per-request shares. Decimal picodollar strings preserve large values; eight breakdown dimensions reconcile to the selected amount, with missing tool allocation represented as Unallocated rather than guessed.

Large roots rebuild in bounded pages while their previous complete generation remains visible, then publish through an atomic generation pointer. Ordinary request changes update the published generation by bounded deltas; structural ownership and role changes rebuild it. Native-history health and source changes invalidate all affected generations. Pending work is visible in summary freshness. Tool-duration baselines and diagnostic snapshots have independent paged cursors, preventing large tool histories from starving request publication. Percentiles use indexed project duration facts, and repeated failure runs remain separate across successful intervening calls. Large-root materialization conservatively reports incomplete coverage until more detailed evidence is available; it does not claim missing host or allocation evidence is complete.

Native disposable API journeys verify shared ownership, stable session-to-tail identity, small-tail and unattributable ledgers, deleted-bead anchors, the reconciliation oracle, read-only requests beside a writer, cached provider outages and bounded source-identity-checked excerpts. A 7,000-card fixture checks stable paging and exact large amounts below the 500 ms feed target. Stress regressions cover continuously changing 5,001-request roots, atomic publication, a 260-request burst, later history catch-up and forward progress with 10,000 long tool calls. Fresh cold review additionally exercised 100,000-call percentile calculation and large publication transactions. Server, browser UI and assembled live acceptance remain subsequent steps.

A read-only copy of the live September-24-and-later Hive/Battlement observation store caught up in 26 bounded summary passes; its feed took 36.52 ms and one cold bead detail took 42.89 ms in-process. These measurements exclude CLI startup and the native Beads panel, which remain part of assembled HTTP acceptance. Full local checks and the final fast check passed; an overlapping earlier fast run hit an operating-system process-group cleanup error after its tests passed and succeeded when repeated independently.

## Dashboard loopback server (hv-l85, step 5)

The resident dashboard command binds IPv4 loopback and retains only HTTP routing, static reads and connection continuity. Every API/page/build invocation selects fresh local-master source. Fixed routes validate identifiers and pass search text as an argv value. Host and browser origin metadata are checked before starting application or build work; responses carry restrictive CSP, no-sniff and no-referrer headers without CORS. Static files use per-component no-follow opens, a fixed MIME table and validated tree/path segments. Header, connection, API concurrency and subprocess deadlines bound local requests.

Builds use the committed UI tree, an exclusive build lock and a package-lock-addressed dependency cache. Node must match the committed pin; npm installs with lifecycle scripts disabled. TypeScript and Vite run in a disposable archive copy. Successful output publishes atomically, while code and environment failures have separate markers and retry rules. A failed newer build falls back to the last successful one with a visible log location. Recent/used build retention and dependency cleanup are bounded by the documented cache policy.

Four black-box journeys use real Git commits and fresh launcher processes with a fake UI toolchain. They cover source updates without resident restart, Python-only bundle reuse, concurrent first loads, failure fallback and explicit retries, source immutability, request rejection before builds, traversal/symlink denial, deep links, static headers, API timeouts, header timeouts, connection saturation and shutdown of build descendants with temporary-copy cleanup. These checks add approximately 32 seconds to the full gate, which previously took 172 seconds in Tollgate; that delivery raised the full deadline to 240 seconds and retained the 30-second fast limit (both later expanded under hv-rcg below). The real React bundle and assembled visual/live acceptance remain subsequent steps.

Live server acceptance exposed a native Tollgate error-channel difference: missing candidate IDs return structured `not-found` JSON on stderr. Observation now checks bounded stdout and stderr error envelopes, so incidental UUID mentions enter the missing-candidate backoff instead of reporting a service outage. No provider text is retained. A native-shaped missing-candidate regression verifies healthy status, no fabricated candidate and no immediate repoll; cold review cleared the correction.

## Dashboard interface and acceptance (hv-286, steps 6–8)

The React/Vite interface now supplies the project rail, responsive feed, all role emblems, window totals, hotspots, URL-backed filters and visible-only 15-second polling. Zod validates response boundaries and BigInt preserves exact picodollar values through display rounding. Feed revisions include later pages; a refresh replaces all loaded pages together. The read-only detail interface includes cumulative spend, ownership bands, source diagnostics, range-filtered and paged requests, eight exact breakdowns, CI attempts with exact-buildset log tails, contributors and sanitized Beads Markdown with copyable native commands. A session reached through a bead works even when all its spend belongs to beads or a small-tail ledger and it therefore has no feed card. Its status and coverage come from observed activity and contributions.

Static mockups were inspected before implementation against Fulcrum primitives, components and feed references. Component journeys exercise exact large values, five distinct emblems, navigation/filters, all breakdowns, range request paging, source reads, failed/passed CI attempts, stale-page replacement, ETag handling, unsupported schemas and hostile Markdown. Native API regressions cover session scopes, time filters, fully-owned session detail, later-page invalidation and exact-attempt plain log reads without persistence. CI provisions the pinned Node runtime and frontend checks join both existing gates.

Assembled browser checks and a real Vite build ran against a disposable copy of the live Hive/Battlement store. The live snapshot's 230 cards and seven-day summary each equal the retained-request oracle exactly; `hv-4up` shows both known owners and eight reconciling dimensions. HTTP feed/detail measured 125/550 ms. An open browser received the new-version pill after a UI commit, Python-only source reused the UI tree, and an intentionally broken preview build served the prior bundle with a warning, all through the same resident process. No failing commit was promoted into production. Evidence and host limitations are recorded in [acceptance](acceptance.md#hive-dashboard-2026-09-25); Tollgate enforces the final full check and synchronized promotion, with candidate identity recorded on the native task closure.

## Dashboard instance replacement (hv-rcg)

`hive dashboard serve` now replaces an existing dashboard on the requested port by default. A user-private Unix socket under `/tmp/hive-dashboard-<uid>/` asks the previous instance to stop; no process is selected from an untrusted PID file or killed merely because it holds the TCP port. A per-port startup lock serializes competing launches, including launches from different configured state directories. The old instance stops accepting HTTP connections, cancels and drains request/build children, then removes its control socket before the replacement binds. Socket identity checks protect cleanup, stale crash sockets recover, and non-Hive listeners still produce a bind error without being terminated.

A real-process regression starts a 60-second build and a 12-second API request, replaces the server within eight seconds, and verifies the request is cancelled, the build process is gone and temporary build copies are removed. It also covers simultaneous launches and recovery after a hard crash; a separate live-socket test preserves an unrelated listener. Cold review found and corrected a Python 3.12 shutdown ordering issue: request cancellation must precede `Server.wait_closed()`, which otherwise waits for active HTTP connections and can exhaust the replacement deadline. The pre-handoff dashboard previously running on this machine was stopped once during this upgrade.

The expanded validation suite now has a 60-second fast limit and a 480-second full limit. The previous 240-second gate expired in unchanged bead-history tests under concurrent host load, after all six dashboard server tests passed; unchanged bead-cost tests took 63.427 seconds versus 40.665 seconds in the retained passing baseline, and Claude tests took 12.641 versus 6.557 seconds. The retry showed the same slowdown and was cancelled before increasing only the overall check limits; all test assertions and application deadlines remain unchanged.

## Executor recovery protocol (hv-79m)

Executor and justiciar now share a same-task recovery protocol before perceived blockers, retry limits, timing misses or resource pressure can end authorized work. It distinguishes stopping identical retries from ending useful diagnosis, permits documented relaxation of agent-imposed constraints within existing authority, preserves explicit user acceptance/pauses/approvals, and requires resumption after successful repair. Genuine remaining external dependencies require checkpoint evidence and an unblocking action before independent-work inspection.

The stop CLI requires an explicit category and, for blocked/pressure stops, a nonempty recovery account before disarming. It stores that account with the reason; this is an attestation, not verification of semantic adequacy or actual role invocation. The bounded corrective prompt uses the same protocol; hook-owned Beads writes and resident services are unchanged. The disposable native lifecycle regression exercises the incident's rejected free-form timing/retry stop, empty recovery rejection, continued feedback, attested external dependency, resume, immediate explicit pause and drained completion alongside existing source reload and interrupt coverage. Command-level checks do not establish live host invocation; that diagnostic acceptance belongs to hv-48p.

The first hv-79m Tollgate attempt passed every completed test, including the executor regression, but exhausted the 240-second full-suite budget during native bead-cost coverage, before history/events/source-routing suites ran. Concurrent native builds were using multiple cores. Same-task justiciar recovery identified this as the agent-set validation deadline introduced with dashboard server coverage, not a user performance acceptance or runtime invariant. The independently developed hv-rcg change landed first with a 480-second full-suite budget and 60-second fast budget; integration retains those values and leaves application deadlines and every test intact. Full validation and synchronized promotion remain required.

Full validation also caught an eager import of executor configuration during basic `hive source` parsing. Stop categories now live in the lightweight executor state module, preserving the existing source-command test's minimal-bootstrap contract; the test remains unchanged.

The executor candidate passed the complete Tollgate suite before integration encountered the concurrent hv-rcg documentation/deadline changes; fresh validation of the reconciled source remains required.

## Executor diagnostic receipts (hv-48p)

The executor retains a private global ring of 128 lifecycle and handler receipts,
bounded to 256 KiB, readable through `executor diagnostics [--session UUID]`.
Receipts identify source commit, timestamp, native session/turn, event, fixed
outcome and stop category without copying prompts or provider/recovery text.
Invocation precedes native reads; final decisions distinguish inactivity, no work,
correction, exhausted correction, new-input supersession and unavailable evidence.
A separate short lock and best-effort warnings keep diagnostics from deciding
execution or becoming a Beads write prerequisite. Prior stops survive resume
until the bounded ring ages them out; this is not an observation registry.

The native disposable lifecycle journey checks retained start/stop/resume and
input/interrupt history, source changes, outage and supersession decisions,
128-receipt retention, private permissions, prompt exclusion and graceful failure
when the diagnostic path is unavailable. Codex CLI 0.154.0's supported `/hooks`
browser showed Stop, UserPromptSubmit and Interrupt each installed, active and
Trusted on 2026-09-25; no hook trust or definitions were changed. At this commit,
command tests establish handler behavior and the native UI establishes current
trust, not host invocation. Post-promotion live exercise results belong in the
native hv-48p acceptance record, including any concrete host activation gap;
missing receipts never establish historical non-invocation.

## Local and hosted validation parity (hv-gog)

The 25 consecutive Linux failures after `242daef2` had two causes: 17 runs rejected a history fixture's hard-coded Pacific repeated hour under a UTC Dolt server, then eight runs stopped earlier at the 500 ms dashboard feed limit (512–935 ms). Tollgate's retained log for the exact `b9c3f075` commit passed both tests on macOS. Identical commands had inherited different environments; local certification did not establish Linux acceptance.

Both gates now use the same preparation/check command and `.python-version` pin (3.12.14), alongside the existing Node, Beads, Dolt and dependency pins. The shared check runner establishes UTC, C locale, UTF-8 I/O and a fixed hash seed. Disposable Dolt fixtures default explicitly to UTC, while the repeated-hour fixture starts its server in America/Los_Angeles. This preserves daylight-saving coverage without depending on the launching machine's timezone.

Feed profiling exposed full scans of `bead_rows` and `bead_replays` for each summary card: concatenating their primary keys in the join prevented indexed lookup. Extracting the bead ID from the summary key instead preserves matching semantics and lets SQLite use both existing primary-key indexes, without migration or cached result changes. The 7,000-card test retains its 500 ms end-to-end limit and now prints measured latency in both gates. Neither performance acceptance nor Tollgate's independent promotion policy is relaxed. Native OS/hardware differences remain; this change controls validation inputs and fixes the observed portability/performance defects, not arbitrary host contention.

At this implementation checkpoint, the UTC-launched repeated-hour regression and dashboard API suite passed; final cold review, full Tollgate certification and the delivered commit's GitHub result remain to be recorded in the native task.

## Archive eligibility and independent deferral review (hv-bu6)

Archivist now treats telemetry links as candidate membership, checks current native assignments separately, skips existing archives, and accepts native idle/terminal-turn evidence without demanding an unavailable queue field. Historical executor links and released deferred beads no longer block cleanup. Project/host scope, fifteen-minute inactivity, actual native archive effects and immediate ownership/activity rechecks remain required, including reversal of a newly archived task if activity raced the operation.

Shared filing and recovery guidance requires explicit agreement from a fresh, read-only justiciar subagent before initial deferral, later deferral or setting/extending its date. The record names the reviewer, evidence, responsible party and resumption condition. Executor, entry and Weaver use that same rule, preserving immediate user pauses while removing automatic deferral for ordinary prerequisites and rejecting superseded approval holds. These are agent workflow requirements, not enforcement in Beads or the stop hook.

The four edited skills pass structural validation, changed relative links resolve, and `scripts/check-fast` passes. Cold review and full Tollgate delivery are recorded on the native bead. No new live deferral or rollback race was manufactured to test the instructions; an end-to-end justiciar deferral decision remains unobserved.

## Validated transcript source identity (hv-mxc)

Discovery now returns a host-specific candidate or a complete previously validated source, with an explicit rejection when no safe locator is available. Collection promotes the locator only after a successful native identity check. Failed candidate reads retain the previous source intact; ambiguous discovery revokes it entirely. A Claude locator identifies a session directory and thread, so a valid child can establish that location while the bad main file's error remains visible. A Codex child cannot validate the root transcript path.

Schema 17 stores the validated locator as one value and removes the independent host/path cache fields. Migration discards all legacy pairs, since even a complete pair may have been assembled from different attempts; it preserves collected usage and per-file cursors. Native discovery must revalidate the cache before outage fallback resumes. Each fallback still runs the host collector's identity and filesystem checks.

Public sweep journeys cover Codex collection, a rejected Claude candidate, index outage and repaired Claude collection without duplicate usage; incomplete and contradictory legacy pairs; ambiguous-source revocation and recovery; and bounded child collection during an index outage despite a bad main file. Focused checks, cold review and full Tollgate certification are recorded on the native bead before closure.

## Shared transcript file lifecycle (hv-b8m)

Codex and Claude collection now use one bounded file/cursor transaction. It owns regular-file opening, replacement/truncation detection, explicit and diagnostic replay, chunk reads, gap persistence, progress calculation and the source upsert. Immutable file/cursor values and distinct successful/failed scan results replace parallel local cursor state. A cursor-write failure rolls back the same transaction's observations and derived allocations.

The host-neutral telemetry store owns database connections; Codex response decoding/persistence and Claude session/subagent preflight remain explicit adapters. The public UsageStore collection method forwards to a collection boundary that retains manual filename inference and Codex root folding. Codex validates its header before resetting a cursor; Claude validates the whole bounded chunk before saving requests or advancing its starting cursor. Per-file parse gaps and host-specific replay, metadata, cost-state and tool-part handling remain visible. Ordinary source selection and resident services require no restart.

The new public collection journey runs against both hosts and crosses a byte-budget boundary, resumes an incomplete record, replaces the file, injects a cursor-write failure and replays without duplicate observations. Existing host, subagent, streamed-pricing, allocation, migration and hot-reload journeys remain in place; focused checks, cold review and full Tollgate evidence are recorded on the native bead before closure.


## Request pricing and tool allocation boundaries (hv-ptd)

Request pricing and best-effort retention now sit below report presentation. Codex and Claude request inputs are distinct immutable values; retained pricing evidence also has host-specific types, with a Codex tier or validated Claude modifiers. Unknown observed modifiers stay unpriced. PricedUsage binds one validated token observation to one retained rate schedule, derives total and components using integer picodollars, and supplies allocation pricing without accepting another usage value. Rate groups use evidence keys rather than zero-total requests. Persisted decimal totals and components remain queryable projections updated with their tool shares in the writer's transaction.

Pure context replay computes token partitions independently of pricing and public breakdown formatting. Schema 18 materializes those partitions in the private allocation_basis table during collection, including unpriced requests, and reconstructs them from saved context facts during migration. Streamed usage or tool-part changes refresh downstream partitions and priced shares together. Retention applies the first retained rates to stored partitions after validating component token counts against the bound usage; it performs no indexed context replay. Missing unindexed legacy partitions are reconstructed independently in one batch, while indexed requests require the basis established by collection or migration. Thread reports replay once for diagnostics. Bead queries retain prices only for historical owner roots and read materialized shares with request totals and typed coverage in one snapshot; global reconciliation still reads all requests.

A disposable cProfile comparison against dc1644f82fe207f4efe88c38cad4de29fcc101c0 used 3 and 30 one-request Claude threads. A cold bead query with one owner previously built 3/30 thread reports and 9/90 tool reports, taking 25/151 ms; it now builds none and performs no context replay, taking 6/5 ms with the same 0.000438000000 USD result. An ownerless bead likewise constructs no reports or replay. A separate cold thread query performs one replay including retention. These disposable profiles establish avoided construction and replay, not a production latency guarantee.

The first full gate caught a bounded-dashboard regression from refreshing whole-thread allocations during price retention. Cold review then strengthened the 5,000-request fixture with context indexes, exposing starvation even when only the new request's charges were written. Materializing token partitions during collection removes that indexed-prefix work from retention. The strengthened dashboard journey retains its original timing/progress assertions for a late request and a 260-request burst. No performance limit was relaxed.

The new mixed-host public journey checks contention disclosures, an empty owner's coverage gap, request component sums, thread/bead/reconciliation totals and materialized tool components before and after stream completion under an edited rate source. Retained rates survive while a new request uses the edited card; unknown modifiers and Claude tier rejection remain explicit. Migration coverage rebuilds the v17 basis without a transcript or repricing. The legacy fixture now encodes a coherent historical rate and total rather than an arbitrary total contradicting its own card. Existing event adoption, shared remainders, pagination, context and source-reload journeys remain required; focused checks, cold review and full Tollgate certification are recorded on the native bead before closure.

## Repository outcome coverage (hv-p6u)

Repository queue, history and check records now seed retained candidates independently of transcript mentions or bead-matched branches; native queue-item events also seed bounded detail reads. Bead attribution still uses the existing transcript-first, branch-fallback rules. Schema 19 forces a repository refresh after upgrade and retains the latest snapshot's history submission bounds, event bounds and discovery counts. All retained outcomes survive native history rollover.

`telemetry outcomes` reports latest outcomes for a half-open submission cohort, unique candidate denominators, explicit retry candidates and distinct validation attempts, with standalone checks excluded. It always identifies the non-exhaustive native history boundary and exposes missing repository observations, unresolved detail reads, staleness and poll errors. It intentionally supplies no full-repository conflict rate or claim of continuous historical coverage. Validated native status arguments provide diagnostic retrieval without retaining free-form provider messages.

A reduced native 24-candidate fixture reconciles to 19 promoted, two conflicts, two validation failures and one cancellation, including previously missing candidate `01a0d978-1703-7ee1-b26d-5f5608d2c328`. Public polling/report journeys cover duplicate appearances, event-only discovery, retries, checks, window edges, schema upgrade, history rollover and malformed refresh rollback; existing tests preserve bead attribution and log exclusion. Cold review and final Tollgate certification are recorded on the bead before closure; historical outcomes already absent from every bounded native response cannot be reconstructed by this change.

## Epic descendant progress (hv-akx)

Epic detail includes a read-only collected descendant view without changing the parent's native state or its direct spend, timeline, or breakdowns. Parent-child edges determine membership, with each reachable bead counted once; child links expose native state, observed CI, holds and prerequisites lacking a completed resolution. Multiple assignments to the same root owner count once, explicitly as assigned owners rather than live processes. Deleted/missing records and renamed aliases do not count as current progress or active work; retained deleted spend remains in the explicit descendant lifetime total. Completion requires both closed status and `hive_resolution=completed`; cancellation is separate.

Implementation/review groups use explicit `hive_work_kind` when available, retained Hearts task codes and Review/Introspect title conventions, then implementation authorization metadata; approval/acceptance gates, nested epics and unclassified work remain Other. The rule is visible in the UI and the scope includes descendant follow-up work, not only original plan packages. Direct epic lifetime spend and descendant lifetime spend stay separate, with incomplete/unpriced/missing observations disclosed. Cached Beads and CI timestamps and collection health qualify observed activity; the detail's live native record may be newer than its descendant snapshot. Epic feed subtitles no longer repeat potentially superseded historical notes.

A disposable native Beads journey covers nested descendants, shared ownership and costs, completed reviews, cancellation as an unresolved prerequisite, holds and recorded running CI; the UI journey checks separate amounts, status, links and stale observation feedback. Read-only live snapshots of Hearts and chess are retained under `~/.local/state/hive/evidence/hv-akx/`: chess showed two active descendants and one distinct owner on 2026-09-26. Current live children were not in CI during that snapshot; the retained running-CI fixture establishes that path. Cold review and full Tollgate certification are recorded on the bead before closure.


Resource evidence is available through explicit `telemetry resource-sample` and `telemetry resources` commands, with no background collection, database migration or admission-policy changes. Bounded regular JSONL inputs retain source hashes, inclusive native CI/process CPU and wall intervals, nested resource wait/held intervals and last-event freshness; half-open comparison windows expose missing coverage and partial intervals rather than allocating CPU by overlap. Host alignment requires matching recorded host identity. Public command fixtures cover duplicate evidence, resource/process identities, unknown CPU, parallel/inclusive measurements, sample failures by window, source immutability and malformed/oversized/FIFO inputs. A real macOS sample captured approximately 50% idle, 55 GiB used memory, zero swap and two running Tollgate candidates; explicitly selected native CI/operation logs produced 19 intervals and correctly exposed unfinished CI terminals and absent nested queue evidence. Capture bounds include provider overhead (macOS CPU is the final one-second top interval), native repository gate counters are kept separate from nested queues and host utilization, and no continuous coverage, causal diagnosis or optimal concurrency is claimed. Linux sampling is implemented but live acceptance was macOS only.

## Dashboard shell and route restoration (hv-0gi.1)

The approved redesign begins with a 216 px rail, shared control spacing and a
compact narrow header that retains every project choice. Existing feed, detail,
session and ledger views remain usable while later children change their layout.
A shared Disclosure supplies expanded state, keyboard operation and focus return.

Each browser history entry retains a validated same-app feed query, loaded extent
and scroll position; returning refetches the complete saved extent coherently
before restoring scroll. Identical query entries own independent caches. Direct
detail loads fall back to the default feed, and arbitrary return URLs or old
sessionStorage values are ignored. Navigation changes no accounting or server
contracts and requires no resident restart. Seventeen frontend behavior tests and
the scoped [shell review](measurements/dashboard-redesign/shell/README.md) cover
this increment; the complete redesign acceptance remains hv-0gi.5. Native cold
review, full Tollgate certification and promoted hot reload are recorded on the
implementation bead.

## Concise dashboard work list (hv-0gi.2)

Newsfeed now separates unboxed selected-window Recorded spend from a title-linked
list of Work, Project, State and Lifetime spend. All roles, ranked issues and
coverage evidence remain available through named disclosures. Every stored title
wraps in full; non-bead rows retain explicit kind qualifiers and unpriced counts.
A narrow lifetime caption preserves scope when column headings are visually hidden.

Search remains visible; Filters contains the original controls, applied count and
reset action without changing backend semantics. The disclosure stays mounted
while a query refetches, preserving open state and the selected project. Existing
cursor paging, coherent loaded-page refresh, safe navigation, accounting and
server contracts remain unchanged. Twenty-two frontend behavior tests and the
[work-list smoke review](measurements/dashboard-redesign/work-list/README.md) cover
this increment; detail reorganization and full redesign acceptance remain pending.
