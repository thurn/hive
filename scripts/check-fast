#!/bin/sh
set -eu
repo_root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
check_venv=${HIVE_CHECK_VENV:-"$repo_root/.venv"}
if [ ! -x "$check_venv/bin/python" ]; then
  echo "Run scripts/prepare-check first." >&2
  exit 1
fi
exec "$check_venv/bin/python" "$repo_root/scripts/check.py" --fast
